1. tts 事件补全
tts 请求容易有歧义，改成这几个，tts 播报请求，tts 缓存检查，tts 缓存命中，tts
  缓存未命中，tts生成请求，tts生成成功，tts生成失败，tts播放开始，tts播放中断，tts播放结束
2. vad 事件
vad 两个事件，onVadStart、onVadEnd
vad start 的时候产生 uuid
vad start 的时候将id同步到端云仲裁器，非最新的 uid 的会话语义被拦截

3. 云端仲裁事件补全
收到asr 命令词，收到 llm 语义，llm｜asr命令词仲裁胜出（原因：优先、超时未收到 llm）， llm｜asr命令词仲裁失败（已经有llm胜出，已经有命令词胜出，不是最新轮会话）

4. 端云仲裁事件补全
收到本地asr 命令词，收到云端语义，云端语义｜本地asr命令词仲裁胜出（原因：优先、超时未收到云端）， 云端语义｜本地asr命令词仲裁失败（已经有云端语义胜出，已经有命令词胜出，不是最新轮会话）

5. 时间线问题
时间线还是混乱，未修复
话语开始 +0 ms source=button ### VAD 分段
+1673 ms durationMs=1440 bytes=46080 segmentCount=2 ### 本地识别
+1710 ms text=打开车窗 durationMs=33 intent=window/power_on ### 云端识别
+2153 ms text=打开车窗。 durationMs=409 ### 大模型
+3122 ms text=打开车窗。 durationMs=968 reply=action:window/power_on ### 云端仲裁
+3122 ms reason=llm_reply route=llm ### TTS 请求
+3171 ms text=好的，车窗已打开 ### TTS 缓存
+3172 ms text=好的，车窗已打开 hit=true bytes=52044 ### 云端仲裁
+3201 ms reason=llm_reply route=llm ### 端云仲裁
+3204 ms reason=cloud_won route=cloud ### 执行
+3206 ms result=applied intent=window/power_on ### TTS 请求
+3206 ms text=好的，车窗已打开 ### 播放
+3332 ms bytes=52044 mime=audio/wav source=network
# AutoVoice