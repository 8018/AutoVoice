# 决策记录：D05b / D07（Neo 拍板，2026-09-12）

> 来源：Neo 于 2026-09-12 11:51 通过助手转达。以下为最终拍板，覆盖 Claude 之前留待的两个设计决策点及其补充。

## 决策

1. **动作账本存储**：独立 SQLite 业务库。**不复用**遥测库，**不自研** WAL。
2. **记账归属**：云端动作由云端执行端记账；手机动作由手机执行端本地记账（双端各自记账，按执行端归属）。
3. **采用与执行分离**：客户端会话层决定「采用」；执行端决定「是否允许执行」；「显示确认」独立于二者。
4. **顺序**：先完成 **D05b** 的「采用 + 候选版本协议」，再接 **D07** 动作执行。
5. **执行边界**：结果未知不自动重试；**真实车控暂不开放**。

## 备注（对 Claude 建议的修订）

- 决策 1 明确为「独立 SQLite 业务库」，与 Claude 推荐 A 一致（独立建库、不混 telemetry），并排除自研 WAL。
- 决策 2 与 Claude 推荐 A（服务端唯一权威、端侧不持久化账本）不同：改为**双端各按执行端记账**。请据此调整账本设计（云端动作→云端账本；手机动作→手机本地账本）。
- 决策 3 进一步拆分「采用（会话层）/ 允许执行（执行端）/ 显示确认（独立）」三层，落实 D07「不能让模型伪造采用凭据」。
