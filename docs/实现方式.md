# AutoVoice

## 
语音链路
录音->信号处理->VAD->意图识别->执行｜播报

##
端侧语音
录音->信号处理->VAD->Asr->nlu->执行｜播报

端侧VLA
录音->信号处理->VAD->VLA
			  图像->VLA->nlu->执行｜播报
		   车辆信息->VLA

##
云端语音
VAD后音频->Asr->nlu->下发到端侧

端侧大模型
VAD后音频->LLM->nlu->下发到端侧

端侧MA
VAD后音频->MA->nlu->下发到端侧

端侧S2S
VAD后音频->S2S模型->nlu｜音频->下发到端侧

##
裁剪规则
云端MA和云端大模型和云端S2S 两两互斥，他们不会同时存在
云端大模型必须和云端语音组合到一起
端侧两个只能二选一
根据法规有可能只有端侧没有云端